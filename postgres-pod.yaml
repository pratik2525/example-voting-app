apiVersion: v1
kind: Pod
metadata:
  name: postgres-app-pod
  labels: 
    name: postgres-app-pod
    app: demo-voting-app
  spec: 
    container:
      -name: postgres
       image: postgres
       ports: 